# Audit forensique Sameva — Maintien en condition opérationnelle

**Date de l'audit :** 2026-07-29
**Périmètre :** lecture seule, aucune modification / commit / push.
**Branche :** `main` (dernier commit `d39b13b`, 2026-07-22)
**Objet :** préparation du document de MCO (échéance 2026-08-17).

Convention : chaque affirmation est sourcée (fichier:ligne ou commande + sortie).
La mention **ABSENT** signale une information cherchée mais non trouvée.

---

## 1. Dépendances

### 1.1 pubspec.yaml (`pubspec.yaml`)

- **Nom / version projet :** `sameva`, `version: 0.1.0+1` (`pubspec.yaml:1`, `pubspec.yaml:5`).
- **Contrainte SDK Dart :** `sdk: '>=3.3.0 <4.0.0'` (`pubspec.yaml:8`).
- **Contrainte Flutter :** ABSENT — aucune contrainte `flutter:` dans `environment` (seul le SDK Dart est borné). Le lockfile note néanmoins `flutter: ">=3.38.0"` et `dart: ">=3.10.0 <4.0.0"` comme versions ayant résolu (`pubspec.lock:1272-1274`).

**Dependencies (`pubspec.yaml:10-50`) — contrainte déclarée → version verrouillée (`pubspec.lock`) :**

| Package | Contrainte pubspec | Verrouillé lock |
|---|---|---|
| flutter | sdk | 0.0.0 (sdk) |
| cupertino_icons | ^1.0.6 | 1.0.8 |
| supabase_flutter | ^2.5.4 | 2.10.3 |
| google_sign_in | ^7.2.0 | 7.2.0 |
| provider | ^6.1.2 | 6.1.5 |
| hive | ^2.2.3 | 2.2.3 |
| hive_flutter | ^1.1.0 | 1.1.0 |
| google_fonts | ^8.1.0 | 8.1.0 |
| flutter_svg | ^2.0.10+1 | 2.2.2 |
| image_picker | ^1.0.7 | 1.2.1 |
| http | ^1.2.0 | 1.4.0 |
| uuid | ^4.2.2 | 4.5.1 |
| intl | ^0.19.0 | 0.19.0 |
| flutter_dotenv | ^5.1.0 | 5.2.1 |
| flutter_local_notifications | ^17.2.4 | 17.2.4 |
| timezone | ^0.9.4 | 0.9.4 |
| share_plus | ^10.1.4 | 10.1.4 |
| url_launcher | ^6.3.0 | 6.3.2 |

**Dev_dependencies (`pubspec.yaml:52-59`) :**

| Package | Contrainte | Verrouillé |
|---|---|---|
| flutter_test | sdk | 0.0.0 (sdk) |
| flutter_lints | ^2.0.0 | 2.0.3 |
| mocktail | ^1.0.4 | 1.0.4 |
| hive_generator | ^2.0.1 | 2.0.1 |
| build_runner | ^2.4.7 | 2.4.13 |

### 1.2 `flutter pub outdated` (extrait de sortie)

Bilan chiffré (dernières lignes de la commande) :
- « **61 upgradable dependencies are locked (in pubspec.lock) to older versions.** »
- « **12 dependencies are constrained to versions that are older than a resolvable version.** »

Écarts majeurs relevés (Current → Resolvable/Latest) : `supabase 2.10.0 → 2.14.0`, `realtime_client 2.6.0 → 2.11.0`, `postgrest 2.5.0 → 2.8.0`, `storage_client 2.4.1 → 2.6.0`, `share_plus_platform_interface 5.0.2 → 7.2.0`, `xml 6.5.0 → 7.0.1`, `win32 5.13.0 → 6.3.0`, `vector_graphics 1.1.19 → 1.2.2`.

Trois paquets **transitifs discontinués** signalés par la commande :
- `build_resolvers` (discontinued)
- `build_runner_core` (discontinued)
- `js` (discontinued)

Ces trois paquets sont tirés par la chaîne `build_runner` / `source_gen` (voir 1.3).

### 1.3 build_runner / hive_generator réellement utilisés ?

Recherche dans `lib/` :
- `@HiveType`, `@HiveField`, `@JsonSerializable`, `part '*.g.dart'` : **0 occurrence** (Grep sur `lib/`).
- Fichiers `*.g.dart` : **0 fichier** (Glob `**/*.g.dart` → « No files found »).

**Conclusion :** `build_runner` et `hive_generator` sont des **dépendances mortes**. Le projet sérialise en JSON manuel (`toJson`/`fromJson`), conforme à la note de `CLAUDE.md` (« Hive uses plain JSON … no code generation »). Ce sont elles qui font entrer les 3 paquets discontinués ci-dessus dans le graphe. Elles peuvent être retirées de `dev_dependencies` sans impact fonctionnel.

### 1.4 Dépendances déclarées mais jamais importées

Vérification par recherche `package:<nom>` dans `lib/` :

| Package | Importé dans lib/ | Preuve |
|---|---|---|
| google_fonts | **Oui** (11 fichiers) | ex. `lib/ui/widgets/common/rarity_badge.dart` |
| flutter_svg | **Oui** (1 fichier) | `lib/ui/widgets/common/item_icon.dart` |
| share_plus | **Oui** (2 fichiers) | `lib/ui/pages/profile/profile_page.dart`, `lib/ui/pages/settings/settings_page.dart` |
| timezone | **Oui** (1 fichier) | `lib/domain/services/notification_service.dart` |

Les quatre paquets ciblés par la consigne sont donc bien utilisés. **Aucune dépendance runtime déclarée n'est jamais importée** ; les seules dépendances inutiles sont les dev-dependencies `build_runner` + `hive_generator` (§1.3).

---

## 2. Historique Git et versions

- **Total commits sur main :** **268** (`git rev-list --count main`).
- **Premier commit :** `1a42050` — 2025-05-25 — « Initial commit - bas Flutter project for Sameva ».
- **Dernier commit :** `d39b13b` — 2026-07-22 — « chore: update Kotlin plugin version to 2.1.20 ».

### 2.1 Tags (`git for-each-ref … refs/tags`)

| Tag | Date création | Type |
|---|---|---|
| v1.0.0 | 2026-07-20 | commit (léger) |
| v1.0.1 | 2026-07-20 | commit (léger) |
| v1.0.3 | 2026-07-21 | commit (léger) |
| v1.0.4 | 2026-07-22 | commit (léger) |

Remarques : **`v1.0.2` est ABSENT** (saut de numérotation). Tous les tags sont **légers** (pointent un commit, pas un tag annoté).

### 2.2 CHANGELOG

`CHANGELOG.md` : **ABSENT** (Glob + `ls` → « ABSENT CHANGELOG.md »). Le suivi de version repose donc uniquement sur les tags Git et sur les *release notes* auto-générées par le workflow Release (`generate_release_notes: true`, `.github/workflows/release.yml:103`).

### 2.3 Version courante

`version: 0.1.0+1` (`pubspec.yaml:5`).

**Incohérence de versionnement :** la version applicative `0.1.0+1` n'a jamais été alignée sur les tags de release `v1.0.x`. Un APK produit par le pipeline Release porte donc en interne `versionName 0.1.0` / `versionCode 1` quel que soit le tag publié (le workflow ne réécrit pas `pubspec.yaml`, voir §3).

### 2.4 40 derniers commits (`git log -40`, format `hash|date|message`)

Aucun des 40 derniers commits ne porte le préfixe `fix:` (Conventional Commits). Les correctifs de la période sont libellés autrement (`fix(...)` plus anciens, ou intégrés via merges de branches `fix/*`, ex. `dbb674b` « Merge pull request #8 from SamymaS/fix/stripe-entitlement-refresh »).

| Hash | Date | Message | Correctif ? |
|---|---|---|---|
| d39b13b | 2026-07-22 | chore: update Kotlin plugin version to 2.1.20 | |
| ee13937 | 2026-07-22 | feat(auth): implement Google Sign-In integration and UI components | |
| 27b9963 | 2026-07-21 | feat(auth): ajout de l'authentification anonyme et mise à niveau invité vers email | |
| fc59be2 | 2026-07-20 | Add ACCESS_NETWORK_STATE permission to AndroidManifest.xml | |
| a00d711 | 2026-07-20 | Add INTERNET permission to AndroidManifest.xml | |
| 71ac861 | 2026-07-20 | Update release workflow to include no-tree-shake-icons | |
| a9cec99 | 2026-07-20 | Refactor environment file generation in release.yml | |
| 6523c65 | 2026-07-16 | Add GitHub Actions workflow for Edge Functions deployment | |
| 7db2cdf | 2026-07-16 | Add GitHub Actions workflow for application release | |
| 63f7e12 | 2026-07-16 | Refactor coverage summary in CI workflow | |
| 2420605 | 2026-07-16 | Create .env.local.example for local setup | |
| e997c04 | 2026-07-16 | Create .env.example for application configuration | |
| 460e3a9 | 2026-07-16 | Rename 0003 game event bus.md → 0003-game-event-bus.md | |
| 54ee4fd | 2026-07-16 | Add ADR for hero avatar | |
| c7f9f95 | 2026-07-16 | Rename 0004 hero avatar.md → 0003-game-event-bus.md | |
| b724c58 | 2026-07-16 | Rename 0002 asset resolver.md → 0002-asset-resolver.md | |
| ac233e8 | 2026-07-16 | Rename 0001 cycle de vie authentification.md → 0001-… | |
| d4cd0e1 | 2026-07-16 | Update documentation for authentication lifecycle | |
| 7ab7d3d | 2026-07-16 | Delete docs/adr/0001-phase-zero-restructuration.md | |
| 18fbbf1 | 2026-07-16 | Add ADR for HeroAvatar widget implementation | |
| 8854db5 | 2026-07-16 | Add ADR for GameEventBus to decouple logic and animations | |
| ff54b46 | 2026-07-16 | Implement AssetResolver for asset path management | |
| 07acf89 | 2026-07-16 | Revise technical debt documentation and entries | |
| d165802 | 2026-07-16 | Add README for architectural decision records | |
| 959a3cc | 2026-07-16 | Add ADR for uniform authentication lifecycle | |
| b86cdea | 2026-07-16 | Delete doc/ANALYSE_PHASE_MVP.md | |
| 5364e25 | 2026-07-16 | Delete doc/ameliorations.md | |
| 9ad3ccc | 2026-07-16 | Delete doc/MVP_SAMEVA.md | |
| b48ba72 | 2026-07-16 | Delete doc/bloc 1.pdf | |
| 47104bb | 2026-07-16 | Delete doc/bloc 2.pdf | |
| 684d81f | 2026-07-16 | Delete doc/bloc 3.pdf | |
| 6636c0c | 2026-07-16 | Delete doc/recap blocs.pdf | |
| 21c0515 | 2026-07-16 | Delete doc/bloc 4.pdf | |
| d8cf14c | 2026-07-16 | Delete documentation/ROADMAP_FEATURES.md | |
| 3a9533d | 2026-07-16 | Delete documentation/VISION_REBOOT_MVP.md | |
| 621ca29 | 2026-07-16 | Update README.md | |
| 9e63479 | 2026-07-16 | Revise Supabase setup guide for Sameva | |
| 327bb01 | 2026-07-16 | Revise Supabase Edge Functions deployment guide | |
| 2f94432 | 2026-07-16 | Revise IA validation document with new structure | |
| 7df22c3 | 2026-07-16 | Revise test coverage documentation | |

**Correctifs identifiables au-delà de la fenêtre de 40 (via `git log --grep`) :** `dbb674b` (merge `fix/stripe-entitlement-refresh`, 2026-06-30), `89cc6a5` « fix(routing): inverse les conditions auth/onboarding dans _AuthGate », `d9b5216` « … + fix race condition post-signup ».

---

## 3. Intégration et déploiement continus (`.github/workflows/`)

Trois workflows présents : `ci.yml`, `deploy.yml`, `release.yml`.

### 3.1 `ci.yml` — « Sameva CI »

- **Déclencheurs :** `push` sur `main`/`develop`, `pull_request` sur `main` (`ci.yml:3-7`).
- **Job unique `analyze-and-test`** (ubuntu-latest) :
  1. Checkout + install Flutter stable avec cache (`ci.yml:14-21`).
  2. Génère `.env` depuis les secrets `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `VALIDATION_AI_URL=` vide (`ci.yml:23-30`).
  3. `flutter pub get` (`ci.yml:32-33`).
  4. **`flutter analyze --no-fatal-warnings`** (`ci.yml:35-36`).
  5. **`flutter test --coverage`** (`ci.yml:38-39`).
  6. Calcul du taux de couverture depuis `coverage/lcov.info` et écriture dans le `GITHUB_STEP_SUMMARY` (`ci.yml:41-65`).
- **Ce qu'il vérifie réellement :** analyse statique + tests + calcul indicatif de couverture. Pas de build applicatif.

### 3.2 `deploy.yml` — « Deploy backend »

- **Déclencheurs :** `push` sur `main` limité aux chemins `supabase/functions/**` et au workflow lui-même, plus `workflow_dispatch` (`deploy.yml:6-12`).
- **Étapes :** lecture du `project_id` depuis `supabase/config.toml` → install CLI Supabase → **`supabase functions deploy`** (les 5 fonctions) → **smoke test** : `POST` corps vide sur `analyze-quest-proof`, échec si code `404`/`000` (`deploy.yml:42-68`).
- **Ce qu'il vérifie :** déploiement effectif + une fonction répond. Pas d'analyze/test/build côté client.

### 3.3 `release.yml` — « Release »

- **Déclencheurs :** `workflow_dispatch` (saisie de version) **ou** `push` d'un tag `v*` (`release.yml:9-18`).
- **Étapes :** validation du format `vX.Y.Z` → Java 17 → Flutter stable → génération `.env` (SUPABASE_URL, SUPABASE_ANON_KEY, VALIDATION_AI_URL, SUGGEST_QUESTS_URL dérivées) → `flutter pub get` → **`flutter analyze --no-fatal-warnings`** → **`flutter test`** (porte bloquante, `release.yml:86-88`) → **`flutter build apk --release --no-tree-shake-icons`** → renommage `sameva-<version>.apk` → publication GitHub Release avec APK attaché (`release.yml:96-113`).

### 3.4 Seuil de couverture

**ABSENT.** `ci.yml` calcule et affiche un taux (`ci.yml:50-56`) mais **ne l'applique jamais comme porte** : en l'absence de `coverage/lcov.info` la commande fait `exit 0` (`ci.yml:44-47`), et aucune comparaison à un plancher n'est faite. Aucun seuil dans les trois workflows.

### 3.5 APK release : pipeline ou local ?

**Produit par le pipeline.** `release.yml:90-102` compile `flutter build apk --release` et l'attache à la Release GitHub. À noter : le workflow **ne modifie pas `pubspec.yaml`** — la version applicative embarquée reste `0.1.0+1` (§2.3) même pour un tag `v1.0.4`.

---

## 4. Supervision existante

### 4.1 ActivityLogService (`lib/domain/services/activity_log_service.dart`)

- **Stockage :** Hive box `settings`, clé fixe `activity_log` (`activity_log_service.dart:37-43`).
- **Capacité :** **50 entrées** max, FIFO (les plus anciennes tronquées, `_maxEntries = 50`, `activity_log_service.dart:39`, `:68`).
- **Types d'entrées :** enum `ActivityType { quest, levelUp, item, achievement, streak }` (`activity_log_service.dart:5`).
- **Alimenté par `addEntry()` depuis 3 emplacements :** `lib/presentation/view_models/player_view_model.dart`, `lib/presentation/use_cases/complete_quest_use_case.dart`, `lib/presentation/use_cases/daily_reset_use_case.dart`.
- **Nature :** journal **local uniquement** (Hive), purgé à chaque logout (`auth_view_model.dart:369`). Ce n'est pas un mécanisme de télémétrie exportée.

### 4.2 debugPrint

**59 occurrences dans 22 fichiers** (Grep `lib/`). Concentrées dans les ViewModels et repositories (ex. `notification_service.dart` ×8, `ai_validation_credits_service.dart` ×8, `player_view_model.dart` ×5, `quest_view_model.dart` ×5). Il s'agit de traces console de développement, sans collecte ni persistance.

### 4.3 Blocs try/catch / logger structuré

Les repositories et services suivent un pattern « best-effort » : try/catch absorbant les erreurs réseau/Hive avec `debugPrint`, sans propagation (ex. `ai_validation_credits_service.dart:187-190`, `:205-209`, `:568-573` ; `activity_log_service.dart:71-73`). **Aucun logger structuré** (pas de package `logging` utilisé côté app, pas de niveaux, pas de sortie fichier).

### 4.4 SDK de crash reporting

**Non.** Recherche `sentry|crashlytics|firebase_crashlytics` dans les `*.yaml` du dépôt : **0 occurrence**. Ni `sentry_flutter` ni `firebase_crashlytics` ne figurent dans `pubspec.yaml`/`pubspec.lock`. Aucune remontée de crash automatisée.

### 4.5 Edge Functions (5) et healthcheck

| Fonction | Rôle | Sécurité |
|---|---|---|
| `analyze-quest-proof` | Validation IA d'une preuve photo de quête (Claude Haiku Vision, « MougiBot ») → score 0-100 (`index.ts:1-24`) | JWT |
| `suggest-quests` | Génération IA de quêtes adaptées au profil joueur (« MougiBot » mode suggester) | JWT |
| `create-checkout-session` | Création d'une Stripe Checkout Session (abonnement), renvoie l'URL (`index.ts:1-25`) | verify_jwt = true |
| `delete-account` | Suppression RGPD du compte (service_role, ordre FK-safe) (`index.ts:1-25`) | verify_jwt |
| `stripe-webhook` | Réception webhooks Stripe, vérif signature HMAC sur raw body, met à jour `premium_subscriptions` (service_role) | Signature HMAC (sans JWT) |

**Healthcheck dédié : ABSENT.** Aucune des 5 fonctions n'est un endpoint de santé. Le seul contrôle de vivacité est le **smoke test** du workflow `deploy.yml:50-68` qui interroge `analyze-quest-proof` avec un corps vide et vérifie qu'elle ne renvoie pas `404`.

### 4.6 Mesure de latence côté client (IA / Supabase)

**ABSENT.** Recherche `Stopwatch|elapsedMilliseconds|latenc|responseTime` : les seules occurrences `Stopwatch` sont dans les mini-jeux (`reaction_game_page.dart`, `memory_game_page.dart`, chronométrage de gameplay). Dans `ai_validation_credits_service.dart:475` et `auth_view_model.dart:379`, le mot « latence » n'apparaît que **dans des commentaires**, sans instrumentation. Aucun timing n'est mesuré ni journalisé sur les appels IA ou Supabase.

---

## 5. Collecte des anomalies

### 5.1 `BACKLOG.md` (intégral)

> **État :** 19/05/2026. Maintenu manuellement à chaque fin de session.
>
> **Priorité haute — dette architecturale :**
> 1. **Refonte des sources de vérité ViewModels** — pattern observé 3 fois (Companions, Quest validation « Quête introuvable », « No element »). Cause : snapshots in-memory parallèles non synchronisés. **[Résolu Quest — 11/06/2026]** : `QuestViewModel` global = source unique. **Reliquat : Player** (à aligner, couplé dette Hive vs Remote) ; **Cat** déjà conforme.
> 2. **Migration initial_schema versionnée** — la table `companions` et d'autres viennent d'un schéma initial non présent dans `supabase/migrations/`. Sur nouvelle instance, les migrations B1/B2 échoueraient sur tables inexistantes. Fix : créer `supabase/migrations/20260101000000_initial_schema.sql`.
>
> **Priorité moyenne :**
> 3. Feature delete account RGPD (bouton Paramètres, Edge Function service_role + cleanup Hive + cascade DB).
> 4. Wording erreur MougiBot 529 (distinguer « réseau down » vs « API IA surchargée » 529 Anthropic).
> 5. Race condition rarity gacha — Dart `_validRarities` inclut `'special'` absent du CHECK SQL `companions_rarity_check` (qui a `'veryRare'`). À aligner avant gacha.
>
> **Priorité basse — nettoyage :**
> 6. Suggestions reviewer B2 (S-1 warning `_persist()` CatViewModel ; S-4 `response as List` → `is List` CatRepository).
> 7. Naming `primaryTurquoise` → pointe sur du violet `0xFF805AD5` dans `app_colors.dart`.
> 8. Colonne `mood` résiduelle dans `companions` (héritée schéma RPG, non mappée Dart, DROP à faire).
> 9. Settings catch-all box Hive (`quest_detail_sheet` persiste des sous-tâches dans box `settings` ; créer box dédiée).
> 10. Tests `AuthVM.waitForSignedInUserId` skippés (Supabase.instance singleton non-injectable).
> 11. **Pin version Flutter (FVM ou équivalent)** — bug pipeline 19/05/2026 (assertion Flutter ListTile durcie sur runner CI mais pas en local). Divergence de versions Flutter = bugs CI imprévisibles.
>
> **Historique de résolution :** 16/05 sync companions B2 ; 19/05 fix quest-validation + patch cat-vm ; 19/05 fix pipeline CI quest_detail_sheet (DecoratedBox → Material) ; 11/06 refonte source de vérité Quest ; 11/06 navbar 5 onglets MVP.

*(Contenu reproduit ; le fichier daté du 19/05/2026 n'a pas été mis à jour depuis, alors que plusieurs de ses items ont évolué — voir §7.)*

### 5.2 `.github/ISSUE_TEMPLATE/`

**ABSENT** (`ls .github/ISSUE_TEMPLATE/` → « ABSENT .github/ISSUE_TEMPLATE »). Aucun template d'issue.

### 5.3 Autre document de suivi des bugs dans `documentation/`

**ABSENT.** `documentation/` ne contient aucun fichier de suivi d'anomalies (liste en §5.4). Le suivi des bugs se limite à `BACKLOG.md` (racine) et aux ADR (`docs/adr/`). Un fichier `TECH_DEBT.md` est référencé par `BACKLOG.md:24` mais **n'existe pas** à la racine ni dans `documentation/` (non trouvé par Glob).

### 5.4 Inventaire `documentation/` (date = dernier commit Git touchant le fichier)

| Dernière modif (Git) | Fichier |
|---|---|
| 2026-07-16 | `documentation/ARCHITECTURE.md` |
| 2026-07-16 | `documentation/CI_CD.md` |
| 2026-07-16 | `documentation/COUVERTURE_TESTS.md` |
| 2026-07-16 | `documentation/IA_ANALYSE_IMAGE.md` |
| 2026-07-16 | `documentation/README.md` |
| 2026-07-16 | `documentation/SUPABASE_EDGE_FUNCTION_IA.md` |
| 2026-07-16 | `documentation/SUPABASE_SETUP.md` |
| 2025-12-13 | `documentation/supabase_schema.sql` |

`supabase_schema.sql` (2025-12-13) est de loin le plus ancien : il **précède** les migrations Stripe/premium/crédits IA de juin 2026 et ne reflète donc plus le schéma réel.

---

## 6. Qualité et tests

### 6.1 `flutter analyze`

Sortie : **« No issues found! (ran in 54.7s) »**. Zéro erreur, zéro warning.

### 6.2 `flutter test`

Sortie finale : **« 00:19 +505: All tests passed! »**
- **Tests exécutés : 505**, tous **verts**, aucun échec.
- **Durée : ~19 s.**

### 6.3 Taux de couverture

**Non mesuré dans cet audit** (lecture seule ; `flutter test` a été lancé sans `--coverage` pour éviter d'écrire `coverage/lcov.info` dans l'arbre de travail). Le pipeline CI le calcule à titre indicatif (§3.4) mais sans plancher. La documentation `documentation/COUVERTURE_TESTS.md` (2026-07-16) peut contenir une valeur de référence à recouper.

### 6.4 Volume de code

- **Fichiers `.dart` dans `lib/` : 100** (`find lib -name "*.dart" | wc -l`).
- **Total lignes `lib/` : 29 218** (`cat` de tous les `.dart` de `lib/`).

---

## 7. Candidats pour la fiche de consignation

### 7.1 Entitlement premium non rafraîchi après paiement Stripe / paywall accessible jusqu'au redémarrage

**Statut : CORRIGÉ — correctif intégré (2026-06-30).**

- Merge `dbb674b` « Merge pull request #8 from SamymaS/fix/stripe-entitlement-refresh » ; commit `81a166a` « feat: enhance premium checkout flow with entitlement polling and UI guards ».
- Mécanique en place (`lib/presentation/view_models/ai_validation_credits_service.dart`) :
  - `onAppResumedAfterCheckout()` (`:486-528`) — au retour dans l'app, poll à backoff progressif (8 tentatives, ~30 s) jusqu'à ce que `isPremium` bascule, protégé contre les polls concurrents (`_pollInProgress`).
  - Déclenché par le lifecycle `AppLifecycleState.resumed` dans `lib/app.dart:77-82`.
  - `refreshEntitlement()` (`:418-422`) relit l'entitlement à la demande, propage via `notifyListeners()` — **sans redémarrage**.
  - `isPremium` est lu **directement** de la colonne `is_premium` (`premium_subscription_repository.dart:48-77`), jamais déduit de `premium_until`, ce qui couvre le délai entre webhook et mise à jour de la date.
- Backend : `stripe-webhook` vérifie la signature HMAC et met à jour `premium_subscriptions` en service_role (`supabase/functions/stripe-webhook/index.ts`).
- Fichiers concernés : `ai_validation_credits_service.dart`, `app.dart`, `premium_subscription_repository.dart`, `supabase/functions/stripe-webhook/`, migration `supabase/migrations/20260616000002_premium_subscriptions.sql`.

### 7.2 Échec silencieux de Google Sign-In lié à la config OAuth Android manquante

**Statut : PARTIEL — code intégré (2026-07-22), configuration de déploiement incomplète.**

- Fonctionnalité implémentée par `ee13937` « feat(auth): implement Google Sign-In integration and UI components ».
- Côté app : `AuthViewModel.signInWithGoogle()` (`auth_view_model.dart:180-203`) traite l'annulation utilisateur comme un échec **silencieux** volontaire (`GoogleSignInExceptionCode.canceled` → pas de message). Le SDK est initialisé avec `serverClientId: SupabaseConfig.googleWebClientId` (`auth_repository.dart:30-31`).
- `google-services.json` **présent** (`android/app/google-services.json`).
- **Faille résiduelle :** `SupabaseConfig.googleWebClientId` **lève une exception** si `GOOGLE_WEB_CLIENT_ID` est absent du `.env` (`supabase_config.dart:42-52`). Or ce paramètre est présent dans `.env.example:18` mais **n'est généré ni par `ci.yml` ni par `release.yml`** (ces workflows n'écrivent que `SUPABASE_URL`, `SUPABASE_ANON_KEY`, et les URLs de fonctions — `release.yml:66-76`). Un APK de release construit par le pipeline n'embarque donc pas `GOOGLE_WEB_CLIENT_ID` → l'initialisation Google Sign-In échoue à l'exécution. Le correctif applicatif est en place, mais la **chaîne de build reste à compléter** pour lever définitivement l'anomalie en production.
- Fichiers concernés : `lib/data/repositories/auth_repository.dart`, `lib/presentation/view_models/auth_view_model.dart`, `lib/config/supabase_config.dart`, `.env.example`, `.github/workflows/release.yml`, `.github/workflows/ci.yml`.

### 7.3 Portefeuille (crédits IA) non rechargé en session restaurée

**Statut : ABSENT — non corrigé (anomalie active plausible).**

- `AiValidationCreditsService.load(userId)` n'est appelé **qu'à un seul endroit** : `_onSignedIn()` (`ai_validation_credits_service.dart:644-650`), lui-même branché sur le stream `onSignedIn`.
- Ce stream n'émet **que** sur `AuthChangeEvent.signedIn` (`auth_view_model.dart:42-46`). Les événements `initialSession` et `tokenRefreshed` (cas d'une **session persistée** rouverte à froid) ne déclenchent pas `onSignedIn` (`auth_view_model.dart:47-58`, branche `else → return`).
- **Contraste avec les autres ViewModels per-user :** dans `main.dart`, `InventoryViewModel`, `EquipmentViewModel` et `CatViewModel` reçoivent un **appel de chargement explicite au boot** (`..loadInventory()` `main.dart:87`, `..loadEquipment()` `:88`, `..loadCats()` `:100`) précisément pour couvrir la session restaurée. **`aiCreditsService` n'a aucun appel `load()` équivalent** (`main.dart:105-111`) : il dépend exclusivement du stream.
- **Conséquence :** au lancement à froid avec session déjà ouverte, `_state` reste à `AiValidationState.empty()` → `balance = 0` et `isPremium = false` affichés, alors que les données existent en Hive sous `ai_validation_<userId>`, jusqu'à une reconnexion réelle. Aucun commit ni item de backlog ne traite ce cas.
- Fichiers concernés : `lib/main.dart:105-111`, `lib/presentation/view_models/ai_validation_credits_service.dart:644-650`, `lib/presentation/view_models/auth_view_model.dart:37-63`.

### 7.4 Échec CI Supabase Preview lié à la table `quests` hors migration

**Statut : ABSENT / PLANIFIÉ — non corrigé, documenté au backlog.**

- Migrations présentes (`supabase/migrations/`) : `20260224000000_player_stats.sql`, `20260512100000_quest_difficulty_check.sql`, `20260513120000_harden_rls_policies.sql`, `20260516000000_companions_sync_support.sql`, `20260517000000_document_leaderboard_view_security.sql`, `20260616000000_ai_validation_credits.sql`, `20260616000001_fix_ai_validation_credits_schema.sql`, `20260616000002_premium_subscriptions.sql`.
- **Aucune migration ne crée la table `quests`** : la plus ancienne migration est `20260224` (`player_stats`) et **aucun `initial_schema`** n'existe. `20260512100000_quest_difficulty_check.sql` **présuppose** l'existence de `quests` (contrainte `CHECK`/`ALTER`) sans la créer.
- Confirmé par `BACKLOG.md:27-31` (item #2) : « la table companions et autres viennent d'un schéma initial non présent dans `supabase/migrations/`. Si déploiement sur nouvelle instance Supabase → migrations B1, B2 échoueraient sur tables inexistantes. » Le fix planifié (`supabase/migrations/20260101000000_initial_schema.sql`) **n'a pas été créé**.
- Un environnement Supabase Preview (base neuve rejouant les seules migrations versionnées) échouerait donc sur les objets dépendant de `quests`. Le schéma « source » `documentation/supabase_schema.sql` date du 2025-12-13 et n'est pas rejoué par la CI.
- Fichiers concernés : `supabase/migrations/` (manque `initial_schema`), `documentation/supabase_schema.sql`, `BACKLOG.md` item #2.

**Synthèse des statuts :**

| Anomalie | Statut |
|---|---|
| 7.1 Entitlement premium / paywall | Corrigé (intégré) |
| 7.2 Google Sign-In OAuth Android | Partiel (code intégré, pipeline incomplet) |
| 7.3 Portefeuille session restaurée | Absent (non corrigé) |
| 7.4 Table `quests` hors migration | Absent / planifié (backlog #2) |

---

## 8. Écarts avec une documentation de maintenance rédigée en avril 2026

Le document de MCO de référence n'ayant pas encore été fourni, cette section liste les **changements du code postérieurs à avril 2026** susceptibles d'invalider une documentation datée de cette période. À recouper à réception du document.

**Changements structurels majeurs (avec dates Git) :**

1. **Migration Provider → ViewModels achevée (2026-04-05, `f908b31`, `05f7961`)** : le dossier `providers/` a été supprimé ; l'état est désormais porté par des `*ViewModel` (`PlayerViewModel`, `QuestViewModel`, `InventoryViewModel`, `EquipmentViewModel`, `CatViewModel`, `NotificationViewModel`, `AuthViewModel`, `ThemeViewModel`) et des services (`AiValidationCreditsService`). **`CLAUDE.md` et le fichier mémoire décrivent encore « 6 providers » (`AuthProvider`, `QuestProvider`, `PlayerProvider`, …) : terminologie obsolète.** De même l'entrée `lib/app_new.dart` mentionnée dans `CLAUDE.md` n'existe pas — le point d'entrée réel est `lib/app.dart` (`SamevaApp`).

2. **Durcissement RLS + audit difficulté (2026-05-12/13, `d007b41`, `60e2183`)** : RLS renforcée sur 6 tables, vue `leaderboard_view`, migration difficulté 1-4 + table d'audit.

3. **Synchronisation cross-device des companions (mai 2026, B1/B2)**.

4. **Système freemium de crédits IA (2026-06-16, `5b5a15b`, `9db4a22`)** : nouvelle box Hive `aiValidation`, table `ai_validation_credits` + RLS, service source de vérité `AiValidationCreditsService`.

5. **Abonnement premium via Stripe (2026-06-16 → 2026-06-30, `242db1d`, `5c81316`, `81a166a`)** : table `premium_subscriptions`, Edge Functions `create-checkout-session` et `stripe-webhook`, dépendance `url_launcher` pour le checkout externe, polling d'entitlement au resume.

6. **Suppression de compte RGPD (`delete-account`)** : nouvelle Edge Function service_role + purge Hive per-user.

7. **Authentification anonyme + Google Sign-In (2026-07-21/22, `27b9963`, `ee13937`)** : nouvelle dépendance `google_sign_in ^7.2.0`, mode invité, mise à niveau invité→email, `GOOGLE_WEB_CLIENT_ID` requis.

8. **Chaîne CI/CD introduite (2026-07-16, `6523c65`, `7db2cdf`, `63f7e12`)** : les 3 workflows (`ci`, `deploy`, `release`) n'existaient pas avant mi-juillet 2026 — toute doc d'avril décrivant un déploiement manuel est caduque.

9. **Premières releases publiques (2026-07-20 → 22)** : tags `v1.0.0`→`v1.0.4`, APK produit par pipeline. La version applicative `pubspec.yaml` (`0.1.0+1`) n'a pas suivi.

**Points de documentation à corriger en priorité pour le MCO :**
- Terminologie « providers » → « ViewModels » (`CLAUDE.md`, mémoire projet).
- Référence `lib/app_new.dart` → `lib/app.dart`.
- `documentation/supabase_schema.sql` (2025-12-13) ne reflète plus le schéma (manque crédits IA, premium, audit difficulté).
- Absence de `TECH_DEBT.md` pourtant référencé par `BACKLOG.md`.
- `BACKLOG.md` figé au 19/05/2026 alors que plusieurs items ont évolué (delete-account livré, etc.).

---

## Annexe — Commandes exécutées

```
git rev-list --count main                         → 268
git log --reverse (first)                         → 1a42050 2025-05-25
git log -1 main                                   → d39b13b 2026-07-22
git for-each-ref refs/tags                        → v1.0.0/1/3/4
flutter analyze --no-fatal-warnings               → No issues found! (54.7s)
flutter test                                      → +505 All tests passed! (~19s)
flutter pub outdated                              → 61 locked older / 12 constrained older / 3 discontinued
find lib -name "*.dart" | wc -l                   → 100
cat lib/**/*.dart | wc -l                         → 29218
Grep @HiveType|@HiveField|@JsonSerializable|*.g.dart → 0
```

*Fin du rapport — généré en lecture seule, non commité.*
